# システム仕様書：授業サポートボタン (Class Support Button)

## 1. システム概要
### 1.1 目的
「授業サポートボタン」は、大学や学校の講義中において、教員が学生の理解度を匿名かつリアルタイムに把握し、授業のペース調整やコミュニケーションの活性化を図ることを目的とするWebアプリケーションである。

### 1.2 ターゲットユーザー
- **教員（先生）**: 授業ルームを作成し、プロジェクターにQRコードを投影。手元のPCでリアルタイムに集計結果を確認する。
- **学生**: スマホ等でQRコードを読み取り、匿名で理解度やリアクションを送信する。

---

## 2. システム構成（アーキテクチャ）
本システムはサーバーレスアーキテクチャを採用し、スケーラビリティと低遅延なリアルタイム通信を実現する。

- **フロントエンド**: React (v19), Vite
- **UIスタイリング**: Vanilla CSS (CSS Variablesを活用したモダンデザイン)
- **アイコン**: Lucide React
- **リアルタイムデータベース**: Firebase Realtime Database
- **ホスティング**: Vercel

---

## 3. 機能要件

### 3.1 共通機能
- **ルーティング**: SPA（Single Page Application）として機能し、URLパス（`/teacher/:roomId`, `/room/:roomId`）によって役割と参加ルームを判定する。
- **ID生成**: ブラウザの `crypto.randomUUID()`（利用不可時は乱数とタイムスタンプの組み合わせ）を用いて、デバイスごと・教員ごとに一意のID（クライアントID・トークン）を生成する。

### 3.2 教員（先生）向け機能
- **ルーム作成機能**:
  - 重複のない一意の6桁の英数字（例: `APA72P`）のルームIDを自動生成する。
  - ルーム作成と同時に、作成者のブラウザ（localStorage）に「先生トークン（認証キー）」を保存し、データベースにも登録する。
- **ダッシュボード表示機能**:
  - 学生へ共有するためのQRコードおよび参加用URLを表示する。
  - **理解度集計**: 「わかった」「わからない」の割合と人数をプログレスバー形式でリアルタイム表示する。
  - **リアクション集計**: 「へぇー」ボタンが押された総回数をリアルタイム表示する。
  - **統計情報**: 現在の「累計参加人数」「総回答数」「リセット回数」を表示する。
- **リセット機能**:
  - スライドの切り替わり等に合わせて、全学生の回答状態（responses）をクリアし、リセット回数（resetVersion）をインクリメントする。
  - ※参加人数のカウントはリセットせず、累計を維持する。

### 3.3 学生向け機能
- **匿名参加機能**:
  - QRコードを読み込むだけでログイン不要で参加可能。初回アクセス時にlocalStorageへクライアントIDを保存する。
- **理解度送信機能**:
  - 「わかった」「わからない」のいずれかのボタンをタップし、現在のステータスとしてFirebaseへ送信する。
  - 選択した状態は画面上にフィードバックとして表示される（例:「送信しました: わかった」）。
- **リアクション送信機能**:
  - 「へぇー」ボタンをタップするたびに、Firebaseのリアクションカウンターをインクリメント（+1）する。このボタンは何度でも連続して押下可能。
- **自動状態同期（リセット追従）**:
  - 教員がリセットを実行した場合（`resetVersion`が更新された場合）、学生側の画面でも選択状態が自動的に解除され、未回答状態に戻る。
  - リロード（再読み込み）した際も、Firebase上の最新の回答状態を正しく復元する。

---

## 4. データモデル (Firebase Realtime Database)

システムは以下のNoSQL階層構造でデータを保持する。

```json
/rooms
  /{roomId}  (例: APA72P)
    ├── createdAt: 1717550000000          // ルーム作成時のタイムスタンプ
    ├── teacherToken: "uuid-string"       // 教員認証用トークン
    ├── resetVersion: 2                   // リセットが実行された回数
    ├── reactions
    │    └── wow: 45                      // 「へぇー」ボタンの累計押下回数
    ├── participants
    │    ├── {clientId_A}: true           // 参加した学生のリスト（累計）
    │    └── {clientId_B}: true
    └── responses
         ├── {clientId_A}: "understood"   // 学生の現在の回答状況
         └── {clientId_B}: "lost"
```

---

## 5. 非機能要件・セキュリティ要件

### 5.1 パフォーマンス最適化（O(N²)問題の回避）
- 教員側は集計のためにルーム全体のデータ（`rooms/{roomId}`）を監視する。
- 一方、学生側はルーム全体を監視するのではなく、**「リセット回数 (`resetVersion`)」** と **「自分自身の回答 (`responses/{clientId}`)」** の2ノードのみを個別に監視する。これにより、100人以上の学生が同時接続して回答を送信しても、他の学生のデバイスに不要な通信が発生せず、帯域幅とバッテリー消費を極小化している。

### 5.2 セキュリティ（先生画面の保護）
- `/teacher/:roomId` へのアクセス時、ブラウザの localStorage に保存された `teacher-token-${roomId}` と、Firebase上の `teacherToken` が一致するか検証する。
- 一致しない場合（URLを直接推測入力した学生など）はエラー画面を表示し、ダッシュボードの閲覧やリセット操作をブロックする。

### 5.3 学生間のプライバシー保護
- 5.1の通信最適化により、学生のデバイスには「他の学生のIDや回答状況」が一切ダウンロードされないため、ブラウザの通信履歴を解析されても他の学生の回答が漏洩することはない。

### 5.4 可用性・エラーハンドリング
- VercelによるSPAルーティング設定（`vercel.json` による `/` へのリライト）を行い、リロード時の404エラーを防止。
- QRコードリーダーのアプリ内ブラウザ等で `localStorage` の利用が制限されている（`SecurityError`が発生する）環境でも、画面がクラッシュ（白画面化）しないよう、安全なラップ関数（try-catch）経由でアクセスするフォールバック処理を実装。
