# 授業サポートボタン 仕様書

## 1. 概要

### 1.1 目的

授業サポートボタンは、授業中に先生が学生の理解度と反応をリアルタイムに確認するためのWebアプリケーションである。

先生は授業ルームを作成し、学生はQRコードまたは参加URLから匿名で参加する。学生は「わかった」「わからない」の理解度を送信し、理解度とは別に「へぇー」リアクションを何度でも送信できる。

### 1.2 対象ユーザー

- 先生: 授業ルームを作成し、集計結果、参加者数、リアクション数、メモを確認する。
- 学生: スマートフォンやPCから授業ルームに参加し、匿名で理解度とリアクションを送信する。

### 1.3 利用環境

- 先生: PCブラウザでの利用を想定する。
- 学生: スマートフォンブラウザでの利用を主に想定する。
- ホスティング: Vercel
- データ保存: Firebase Realtime Database
- 認証: Firebase Authentication 匿名認証

## 2. 機能仕様

### 2.1 授業ルーム作成

先生はトップページから「授業ルームを作成」を押して新しい授業ルームを作成する。

ルーム作成時に以下を実行する。

- Firebase匿名認証で先生用の匿名ユーザーを作成または取得する。
- 6文字の英数字ルームIDを生成する。
- Firebase Realtime Database の `/rooms/{roomId}` にルームデータを作成する。
- 作成後、先生画面 `/teacher/{roomId}` に遷移する。

### 2.2 先生画面

先生画面では以下を表示する。

- 授業ルームID
- 学生参加用QRコード
- 学生参加用URL
- 参加者数
- 回答数
- リセット回数
- 「わかった」「わからない」の人数と割合
- 「へぇー」リアクションの累計回数
- 先生用メモ欄

先生画面で可能な操作は以下である。

- 参加URLをコピーする。
- メモを保存する。
- 現在の集計にタグを付けてタイムラインに記録し、次の区間へ進む。

### 2.3 学生画面

学生は `/room/{roomId}` から授業ルームに参加する。

学生画面では以下の操作ができる。

- 「わかった」を送信する。
- 「わからない」を送信する。
- 「へぇー」を何度でも送信する。

「わかった」と「わからない」は、同じ学生につき現在の回答を1つだけ保持する。押し直した場合は最新の回答で上書きする。

「へぇー」は理解度とは別のリアクションであり、同じ学生が何度でも押せる。押した回数は全体の累計として加算する。

### 2.4 タイムライン記録

先生が「記録して次へ」を実行すると、現在の集計を履歴として保存してから、次の区間の集計を開始する。

履歴には以下を保存する。

- タグ。例: `スライド1`、`問3`、`小テスト後`
- 先生メモ
- 記録時刻
- 「わかった」の人数
- 「わからない」の人数
- 回答数
- 参加者数
- 「へぇー」回数
- 記録時点のリセット回数

記録後、以下を行う。

- 全学生の理解度回答を削除する。
- 「へぇー」リアクション数を0に戻す。
- `resetVersion` を1増やす。

参加者情報はリセットしない。これにより、授業中に一度参加した学生数を維持できる。

### 2.5 先生メモ

先生は現在の集計がどの質問・話題に対応するものかをメモできる。

- メモは最大140文字。
- 先生画面にのみ表示する。
- 学生画面には表示しない。
- Firebase Realtime Database の `/rooms/{roomId}/note` に保存する。

### 2.6 授業タイムライン

先生画面には、保存済みのタイムライン履歴を新しい順に表示する。

各履歴には以下を表示する。

- タグ
- 記録時刻
- メモ
- 「わかった」の人数
- 「わからない」の人数
- つまずき率
- 「へぇー」回数

つまずき率は、記録時点の回答数に対する「わからない」の割合である。

## 3. 画面仕様

### 3.1 トップページ

URL:

```text
/
```

表示内容:

- アプリ名
- 簡単な説明
- 「授業ルームを作成」ボタン
- エラー発生時のエラーメッセージ

### 3.2 先生画面

URL:

```text
/teacher/{roomId}
```

表示内容:

- 授業ルームID
- リセットボタン
- 参加者数、回答数、リセット回数
- 先生メモ欄
- タイムライン記録欄
- 理解度集計グラフ
- 「へぇー」回数
- 授業タイムライン
- QRコード
- 参加URL

### 3.3 学生画面

URL:

```text
/room/{roomId}
```

表示内容:

- 「わかった」ボタン
- 「わからない」ボタン
- 「へぇー」ボタン
- 現在の送信状態
- エラー発生時のエラーメッセージ

## 4. データ仕様

Firebase Realtime Database には以下の構造で保存する。

```json
{
  "rooms": {
    "{roomId}": {
      "createdAt": 1717550000000,
      "teacherUid": "firebase-auth-uid",
      "note": "問3の解説後",
      "resetVersion": 0,
      "reactions": {
        "wow": 0
      },
      "history": {
        "{historyId}": {
          "label": "スライド1",
          "note": "問3の解説後",
          "createdAt": 1717550100000,
          "counts": {
            "understood": 32,
            "lost": 8
          },
          "wow": 15,
          "totalResponses": 40,
          "participants": 45,
          "resetVersion": 0
        }
      },
      "participants": {
        "{studentUid}": true
      },
      "responses": {
        "{studentUid}": "understood"
      }
    }
  }
}
```

### 4.1 フィールド定義

| フィールド | 型 | 説明 |
| --- | --- | --- |
| `createdAt` | number | ルーム作成時刻のUnix time milliseconds |
| `teacherUid` | string | ルームを作成した先生のFirebase匿名認証UID |
| `note` | string | 先生メモ。最大140文字 |
| `resetVersion` | number | リセット回数 |
| `reactions.wow` | number | 「へぇー」の累計回数 |
| `history.{historyId}` | object | タグ付きで保存された過去集計 |
| `participants.{studentUid}` | boolean | 学生の参加記録 |
| `responses.{studentUid}` | string | 学生の現在回答。`understood` または `lost` |

## 5. 認証・権限仕様

### 5.1 認証方式

Firebase Authentication の匿名認証を使用する。

匿名認証を使う理由は以下である。

- 学生にログイン操作を要求しない。
- 学籍番号や氏名を扱わない。
- Firebase Security Rules でユーザー単位の読み書き制御を行える。

### 5.2 先生権限

ルーム作成時の匿名ユーザーUIDを `teacherUid` として保存する。

先生権限を持つユーザーだけが以下を行える。

- ルーム全体の集計データを読む。
- メモを読む。
- メモを書く。
- タイムライン履歴を作成する。
- 回答をリセットする。
- 「へぇー」回数を0に戻す。

### 5.3 学生権限

学生は匿名認証後、自分のUIDに対応するデータだけを操作する。

学生ができること:

- 自分の回答を読む。
- 自分の回答を書く。
- 自分の参加記録を書く。
- `resetVersion` を読む。
- 「へぇー」回数を読む。
- 「へぇー」回数を1ずつ増やす。

学生ができないこと:

- 他の学生の回答を読む。
- 他の学生の回答を書く。
- ルーム全体の集計を読む。
- 先生メモを読む。
- リセットする。
- `teacherUid` を読む。

## 6. Firebase Security Rules

Firebase Realtime Database のルールは `database.rules.json` に定義する。

主な制約:

- 全体の `.read` と `.write` は `false`。
- ルーム作成は匿名認証済みユーザーのみ可能。
- ルームIDは6文字の英数字。
- 回答値は `understood` または `lost` のみ許可。
- メモは先生のみ読み書き可能で、140文字以内。
- タイムライン履歴は先生のみ読み書き可能。
- リセット操作は先生のみ可能。
- 「へぇー」は認証済みユーザーが1ずつ加算する場合のみ許可。

## 7. 環境変数

Vercelおよびローカル開発環境には以下の環境変数が必要である。

```text
VITE_FIREBASE_API_KEY
VITE_FIREBASE_AUTH_DOMAIN
VITE_FIREBASE_DATABASE_URL
VITE_FIREBASE_PROJECT_ID
VITE_FIREBASE_STORAGE_BUCKET
VITE_FIREBASE_MESSAGING_SENDER_ID
VITE_FIREBASE_APP_ID
```

## 8. デプロイ仕様

### 8.1 Vercel

Vercelでは以下の設定を使用する。

```text
Framework Preset: Vite
Build Command: npm run build
Output Directory: dist
Install Command: npm install
```

`vercel.json` でSPAルーティングのためのrewriteを設定する。

```json
{
  "rewrites": [
    { "source": "/(.*)", "destination": "/" }
  ]
}
```

### 8.2 Firebase

Firebase側では以下を設定する。

- Authentication で匿名ログインを有効化する。
- Realtime Database を作成する。
- `database.rules.json` の内容をRealtime Database Rulesに公開する。

## 9. 非機能要件

### 9.1 プライバシー

- 学生の氏名、学籍番号、メールアドレスは扱わない。
- 学生はFirebase匿名認証UIDで識別する。
- 先生画面には個人別の回答ではなく、集計結果を表示する。

### 9.2 性能

- 先生画面はルーム全体を監視する。
- 学生画面は自分の回答とリセット状態を中心に監視する。
- 「へぇー」はトランザクションで加算し、同時押下時の競合を避ける。

### 9.3 可用性

- VercelとFirebase Realtime Databaseに依存する。
- どちらかが停止している場合、ルーム作成・回答送信・集計表示は利用できない。

## 10. 既知の制限

- 授業履歴の一覧表示はない。
- CSV出力はない。
- 先生が別ブラウザや別端末から同じ先生画面を開く仕組みはない。
- 学生の個別フォロー機能はない。
- メモは現在の区間用に1件保存する。
- タイムライン履歴の削除・編集機能はない。

## 11. テスト観点

- トップページから授業ルームを作成できる。
- 作成後、先生画面に遷移する。
- 先生画面にQRコードと参加URLが表示される。
- 学生画面から「わかった」を送信すると先生画面の集計に反映される。
- 学生画面から「わからない」を送信すると先生画面の集計に反映される。
- 同じ学生が回答を押し直すと、最新回答だけが集計される。
- 「へぇー」を複数回押すと、回数が押した分だけ増える。
- 先生が「記録して次へ」を押すと、現在の集計がタイムラインに保存される。
- 記録後、回答と「へぇー」が0に戻る。
- リセット後も参加者数は維持される。
- 先生メモを保存できる。
- 保存した履歴が先生画面の授業タイムラインに表示される。
- 学生画面から先生メモを読めない。
- 先生以外の匿名ユーザーが先生画面を開けない。
- Vercel上で `/teacher/{roomId}` や `/room/{roomId}` を直接開いても404にならない。
